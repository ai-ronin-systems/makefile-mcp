# Docker/brownfield example

Container commands remain Make recipes. Just Make It MCP (JMIM) does not model Docker or third-party tools as separate execution providers.

For production-style images that provision the required tools and copy/mount the Makefile contract, see [Deployment](../../docs/deployment.md).
